// variables1.rs
//
// Make me compile!
//
// Execute `rustlings hint variables1` or use the `hint` watch subcommand for a
// hint.

fn main () {
    let x = 8;
    let y = 7;
    let z = x + y;
    println!("x + y has the value {}", z );
}
